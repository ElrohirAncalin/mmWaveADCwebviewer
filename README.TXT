Main files are 

index.html (contains the web skeleton)
CSS/adcgraph.css (contains the decoration)
scripts/adcgraph.js (loads the data, calls the plot library, and updates the graph when the inputs are changed)

The rest of folders contain fonts and libraries
